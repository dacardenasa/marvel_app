export type SearchBy = "comics" | "characters";

export type SearchTabs = { comics: "comics"; characters: "characters" };

export const enum SearchCategories {
  comics = "comics",
  characters = "characters"
}
